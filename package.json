{
  "name": "collab-editor",
  "private": true,
  "description": "Real-time collaborative document editor — monorepo root",
  "scripts": {
    "test": "npm --prefix shared test && npm --prefix client test && npm --prefix server test",
    "test:ot": "npm --prefix shared test",
    "test:client": "npm --prefix client test",
    "test:persistence": "npm --prefix server test",
    "start": "node server/src/index.js"
  },
  "dependencies": {
    "redis": "^5.12.1"
  },
  "engines": {
    "node": ">=22.12.0"
  }
}
